import { MinimoValidatorParametroDirective } from './minimo-validator-parametro.directive';

describe('MinimoValidatorParametroDirective', () => {
  it('should create an instance', () => {
    const directive = new MinimoValidatorParametroDirective();
    expect(directive).toBeTruthy();
  });
});
