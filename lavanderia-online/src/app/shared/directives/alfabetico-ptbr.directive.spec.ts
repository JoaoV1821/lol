import { AlfabeticoPTBRDirective } from './alfabetico-ptbr.directive';

describe('AlfabeticoPTBRDirective', () => {
  it('should create an instance', () => {
    const directive = new AlfabeticoPTBRDirective();
    expect(directive).toBeTruthy();
  });
});
