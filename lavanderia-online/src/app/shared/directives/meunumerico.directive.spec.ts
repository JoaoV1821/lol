import { MeunumericoDirective } from './meunumerico.directive';

describe('MeunumericoDirective', () => {
  it('should create an instance', () => {
    const directive = new MeunumericoDirective();
    expect(directive).toBeTruthy();
  });
});
