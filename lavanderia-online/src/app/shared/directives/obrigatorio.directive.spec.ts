import { ObrigatorioDirective } from './obrigatorio.directive';

describe('ObrigatorioDirective', () => {
  it('should create an instance', () => {
    const directive = new ObrigatorioDirective();
    expect(directive).toBeTruthy();
  });
});
